return {
  'numToStr/Comment.nvim',
  event = "VeryLazy",
  config = true,
}
-- Usage:
--   gcc - toggle comment on current line
--   gc  - toggle comment (with motion, e.g., gcap for paragraph)
--   gbc - toggle block comment on current line
